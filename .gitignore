__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.ruff_cache/

# virtual environment
.venv/

# generated simulation data + trained models -- reproducible from
# `make demo`, not source. Includes the live trust ledger, which changes
# on every app run and has no business being versioned.
data/simulated/
data/models/
